// Constants shared by WorldNavigationSession and its method modules.
export const LOCATION_FOCUS_OFFSET = { x: 12, y: 12, z: 12 };

// Worlds within this distance of the camera are loaded and rendered.
export const STREAMING_RADIUS = 150;
