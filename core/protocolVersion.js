// The ForkBuild Protocol version — versioned independently from the
// application/engine version, per docs/Principles.md ("Version the
// protocol independently from the application"). See docs/Protocol.md.
export const PROTOCOL_VERSION = '0.1';
