// The ForkBuild application/engine version — the current milestone
// number from docs/Roadmap.md, shown on the About page
// (ui/views/AboutView.js). Versioned independently from the ForkBuild
// Protocol (core/protocolVersion.js) per docs/Principles.md, "Version
// the protocol independently from the application" — bumping this
// tracks milestone progress and does not imply a protocol change, and
// vice versa.
export const VERSION = {
    major: 0,
    minor: 2,
    patch: 35
};
